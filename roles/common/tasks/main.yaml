- name: delete directory
  ansible.builtin.file:
    path: /app
    state: absent

- name: create directory
  ansible.builtin.file:
    path: /app
    state: directory

- name: download and unzip the code
  ansible.builtin.unarchive:
    src: https://daws.78s.online.com
    dest: src
